<% if mirror.on? && confession %>
  <div class="confession">
    <div>
      <%= emojify wrap_in_span confession.body %>
    </div>
  </div>

  <div class="info">
    <%= confession.user.alias %> bekände detta<br>
    för <%= time_ago_in_words(confession.created_at) %> sen
  </div>
<% end %>
